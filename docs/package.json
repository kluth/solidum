{
  "name": "@solidum/docs",
  "version": "0.1.0",
  "description": "Documentation site for Solidum framework - built with Solidum!",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.js && python -m http.server 8080 --directory dist",
        "build": "pnpm --filter @solidum/core build && pnpm --filter @solidum/utils build && pnpm --filter @solidum/ui build && pnpm --filter @solidum/router build && pnpm --filter @solidum/store build && pnpm --filter @solidum/context build && pnpm --filter @solidum/ssr build && tsc && node build.js"
  },
  "dependencies": {
    "@solidum/core": "workspace:*",
    "@solidum/ui": "workspace:*",
    "@solidum/router": "workspace:*",
    "@solidum/store": "workspace:*",
    "@solidum/context": "workspace:*",
    "@solidum/utils": "workspace:*",
    "@solidum/ssr": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.25.10",
    "typescript": "^5.3.0"
  }
}
